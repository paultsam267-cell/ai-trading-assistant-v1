name: scanner

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Restore last scanner state
        uses: actions/cache/restore@v4
        with:
          path: .scanner-state.json
          key: scanner-state-${{ github.ref_name }}-${{ github.run_id }}
          restore-keys: |
            scanner-state-${{ github.ref_name }}-
            scanner-state-

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install requests

      - name: Run real scanner
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SCAN_CHAINS: "solana,bsc"
          MIN_MARKET_CAP: "80000"
          MAX_MARKET_CAP: "3000000"
          MIN_LIQUIDITY: "30000"
          MIN_H1_VOLUME: "30000"
          MIN_SPIKE_RATIO: "2.5"
          MIN_PRICE_MOVE_PCT: "12"
          MAX_PRICE_MOVE_PCT: "1000"
          BUY_SELL_RATIO_LONG_MIN: "1.05"
          BUY_SELL_RATIO_SHORT_MAX: "0.95"
          ALERT_COOLDOWN_MIN: "240"
          TOP_N: "3"
        run: python scanner.py

      - name: Save scanner state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .scanner-state.json
          key: scanner-state-${{ github.ref_name }}-${{ github.run_id }}
